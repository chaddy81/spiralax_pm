<h3>Recent Discussions</h3>
<table>
<% @discussions.take(5).each do |discussion| %>
  <tr class="<%= cycle('even', 'odd') %>">
    <td><%= discussion.title %></td>
    <td><%= discussion.comment %></td>
  </tr>
<% end %>
</table>
