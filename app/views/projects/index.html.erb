<h1>Projects</h1>

<%= form_tag "/projects/select" do %>
	<%= select_tag "project_stuff", options_from_collection_for_select(@projects, "id", "name", selected = session[:current_project]),:include_blank => true, :onchange => "this.form.submit();" %>
<% end %> 

<h2>Recent Activity</h2>
<%= activity %>

<%= link_to 'New Project', new_project_path %>
