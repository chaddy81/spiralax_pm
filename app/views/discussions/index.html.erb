<h1>Listing discussions</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @discussions.each do |discussion| %>
  <tr>
    <td><%= discussion.title %></td>
    <td><%= discussion.comment %></td>
	<td><%= link_to 'Show', project_discussion_path(@project, discussion) %></td>
    <td><%= link_to 'Destroy', project_discussion_path(@project, discussion), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= button_to 'New Discussion', new_project_discussion_path %>
