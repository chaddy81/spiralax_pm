<%= form_for [@project, @upload], :html => { :multipart => true } do |f| %>
  <div class="field">
    <%= f.file_field :upload %>
  </div>
  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>

<%= link_to 'Back', project_uploads_path %>